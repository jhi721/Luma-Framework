// UI_SolidFillAlpha as translated by dgVoodoo 2.81.3 (ps_4_0, hence a different hash). Same three-instruction body
// (constant colour, interpolated alpha) as 0xD8C503D9, so the whole pass comes from that file.
#include "UI_SolidFillAlpha_0xD8C503D9.ps_5_0.hlsl"
