// UI_GFxSolidAlpha as translated by dgVoodoo 2.81.3 (ps_4_0, hence a different hash). Instruction-equivalent to
// 0x4A4D8050: 2.81.3 hoists the alpha's mov_sat to the top and saturates the whole vector, but only .w is ever read
// (both disassemblies compared), so the whole pass comes from that file.
#include "UI_GFxSolidAlpha_0x4A4D8050.ps_5_0.hlsl"
