// DofBloomGather4 as translated by dgVoodoo 2.81.3 (ps_4_0, hence a different hash). Same 89-instruction stream and
// cb4 rows as 0x28F8DB16, the QualityBloom=FALSE perm (archived disasm), so the whole pass comes from that file.
#include "DofBloomGather4_0x28F8DB16.ps_5_0.hlsl"
