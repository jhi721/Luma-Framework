// UI_GFxTextureTint as translated by dgVoodoo 2.81.3 (ps_4_0, hence a different hash). Instruction-equivalent to
// 0x0B2707E5: 2.81.3 hoists both mov_sat to the top and swaps which temp holds which input, consistently through the
// add/mad and the alpha mul (both disassemblies compared), so the whole pass comes from that file.
#include "UI_GFxTextureTint_0x0B2707E5.ps_5_0.hlsl"
