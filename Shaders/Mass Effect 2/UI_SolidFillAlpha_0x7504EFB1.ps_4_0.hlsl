// Constant-colour fill with interpolated alpha as translated by dgVoodoo 2.81.3 (ps_4_0, hence a different hash). Body is two `mov`s and matches
// UI_SolidFillAlpha_0xD8C503D9 exactly, so the whole pass comes from that file.
#include "UI_SolidFillAlpha_0xD8C503D9.ps_5_0.hlsl"
