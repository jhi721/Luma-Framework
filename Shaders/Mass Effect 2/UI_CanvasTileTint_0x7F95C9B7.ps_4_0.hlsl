// UE3 Canvas tile with a tint and a fade as translated by dgVoodoo 2.81.3 (ps_4_0, hence a different hash). Dump-verified equivalent to
// UI_CanvasTileTint_0x2C849E5C apart from register numbering, `centroid` and rcp-vs-div, so the whole pass comes from that file.
#include "UI_CanvasTileTint_0x2C849E5C.ps_5_0.hlsl"
