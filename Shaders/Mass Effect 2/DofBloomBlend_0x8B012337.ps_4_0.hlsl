// FDOFAndBloomBlend as translated by dgVoodoo 2.81.3 (ps_4_0, hence a different hash). Hashed from the same
// cooked SM3 program as its 2.87.3 twin (GlobalShaders-PC-D3D-SM3.bin), so the whole pass comes from that file.
#include "DofBloomBlend_0x780BCE69.ps_5_0.hlsl"
