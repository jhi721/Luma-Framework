// UberPostProcessBlend, hard-clip perm as translated by dgVoodoo 2.81.3 (ps_4_0, hence a different hash). Dump-verified equivalent to
// UberPost_0x88CBF48C apart from register numbering, `centroid` and rcp-vs-div, so the whole pass comes from that file.
#include "UberPost_0x88CBF48C.ps_5_0.hlsl"
